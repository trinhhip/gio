<?php
/* @var OmnyfyCustomzation\Vendor\Block\Vendor\Listing\Pager $block */
$limit = $block->getPageSize();
?>

<div class="toolbar-top">
    <div class="pager">
        <div class="limiter">
            <strong class="limiter-label">Show</strong>
            <select id="limiter" class="limiter-options">
                <?php foreach ($block->getNumberPageShow() as $numberPage): ?>
                    <option value="<?= $numberPage ?>" <?= $limit == $numberPage ? 'selected="selected"' : '' ?>>
                        <?= $numberPage ?>
                    </option>
                <?php endforeach; ?>
            </select>
            <span class="limiter-text">per page</span>
        </div>
    </div>
</div>
<script>
    require(['jquery','mage/url'],
        function ($, url) {
        $('#limiter').on('change', function () {
            var limit = $('#limiter').val();
            window.location.href = url.build('shop/brands/index/limit/' + limit);
        })
    });
</script>